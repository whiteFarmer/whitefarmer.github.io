/* 在 custom-background.styl 文件中 */
body {
    background-image: url("/img/background.jpg"); /* 你的图片路径 */
    background-size: contain; /* 关键：使用 cover 来避免变形并填满屏幕 */
    background-repeat: no-repeat;
    background-attachment: fixed;
    background-position: center;
}